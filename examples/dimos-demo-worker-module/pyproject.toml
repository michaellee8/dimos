[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dimos-demo-worker-module"
version = "0.1.0"
description = "Dependency-light DimOS Runtime Project example"
requires-python = ">=3.10,<3.13"
dependencies = [
    "inflection>=0.5.1",
]
