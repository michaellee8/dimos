// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Cursor, Read, Write};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct InteractiveMarker {
    pub header: crate::std_msgs::Header,
    pub pose: crate::geometry_msgs::Pose,
    pub name: std::string::String,
    pub description: std::string::String,
    pub scale: f32,
    pub menu_entries: Vec<crate::visualization_msgs::MenuEntry>,
    pub controls: Vec<crate::visualization_msgs::InteractiveMarkerControl>,
}

impl InteractiveMarker {
    pub const HASH: i64 = 0xE9ED8E5F9A72545Fu64 as i64;
    pub const NAME: &str = "visualization_msgs.InteractiveMarker";

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let mut tmphash = self_hash as u64;
        tmphash = tmphash.wrapping_add(crate::std_msgs::Header::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::geometry_msgs::Pose::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::visualization_msgs::MenuEntry::hash_recursive(
            parents,
        ));
        tmphash = tmphash.wrapping_add(
            crate::visualization_msgs::InteractiveMarkerControl::hash_recursive(parents),
        );
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint())
            .unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(
                io::ErrorKind::InvalidData,
                format!(
                    "Hash mismatch: expected {:016x}, got {:016x}",
                    expected, hash
                ),
            ));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        buf.write_i32::<BigEndian>(self.menu_entries.len() as i32)?;
        buf.write_i32::<BigEndian>(self.controls.len() as i32)?;
        self.header.encode_one(buf)?;
        self.pose.encode_one(buf)?;
        {
            let bytes = self.name.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        {
            let bytes = self.description.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        buf.write_f32::<BigEndian>(self.scale)?;
        for v0 in self.menu_entries.iter() {
            v0.encode_one(buf)?;
        }
        for v0 in self.controls.iter() {
            v0.encode_one(buf)?;
        }
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let menu_entries_length = buf.read_i32::<BigEndian>()? as usize;
        let controls_length = buf.read_i32::<BigEndian>()? as usize;
        let header = crate::std_msgs::Header::decode_one(buf)?;
        let pose = crate::geometry_msgs::Pose::decode_one(buf)?;
        let name = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        let description = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        let scale = buf.read_f32::<BigEndian>()?;
        let menu_entries = {
            let mut v = Vec::with_capacity(menu_entries_length);
            for _ in 0..menu_entries_length {
                let _elem_0 = crate::visualization_msgs::MenuEntry::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        let controls = {
            let mut v = Vec::with_capacity(controls_length);
            for _ in 0..controls_length {
                let _elem_0 = crate::visualization_msgs::InteractiveMarkerControl::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        Ok(Self {
            header,
            pose,
            name,
            description,
            scale,
            menu_entries,
            controls,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4;
        size += 4;
        size += self.header.encoded_size();
        size += self.pose.encoded_size();
        size += 4 + self.name.len() + 1;
        size += 4 + self.description.len() + 1;
        size += 4;
        for v0 in self.menu_entries.iter() {
            size += v0.encoded_size();
        }
        for v0 in self.controls.iter() {
            size += v0.encoded_size();
        }
        size
    }
}
