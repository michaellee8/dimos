// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Cursor, Read, Write};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct MultiEchoLaserScan {
    pub header: crate::std_msgs::Header,
    pub angle_min: f32,
    pub angle_max: f32,
    pub angle_increment: f32,
    pub time_increment: f32,
    pub scan_time: f32,
    pub range_min: f32,
    pub range_max: f32,
    pub ranges: Vec<crate::sensor_msgs::LaserEcho>,
    pub intensities: Vec<crate::sensor_msgs::LaserEcho>,
}

impl MultiEchoLaserScan {
    pub const HASH: i64 = 0xF02A6253849D18E2u64 as i64;
    pub const NAME: &str = "sensor_msgs.MultiEchoLaserScan";

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let mut tmphash = self_hash as u64;
        tmphash = tmphash.wrapping_add(crate::std_msgs::Header::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::sensor_msgs::LaserEcho::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::sensor_msgs::LaserEcho::hash_recursive(parents));
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint())
            .unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(
                io::ErrorKind::InvalidData,
                format!(
                    "Hash mismatch: expected {:016x}, got {:016x}",
                    expected, hash
                ),
            ));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        buf.write_i32::<BigEndian>(self.ranges.len() as i32)?;
        buf.write_i32::<BigEndian>(self.intensities.len() as i32)?;
        self.header.encode_one(buf)?;
        buf.write_f32::<BigEndian>(self.angle_min)?;
        buf.write_f32::<BigEndian>(self.angle_max)?;
        buf.write_f32::<BigEndian>(self.angle_increment)?;
        buf.write_f32::<BigEndian>(self.time_increment)?;
        buf.write_f32::<BigEndian>(self.scan_time)?;
        buf.write_f32::<BigEndian>(self.range_min)?;
        buf.write_f32::<BigEndian>(self.range_max)?;
        for v0 in self.ranges.iter() {
            v0.encode_one(buf)?;
        }
        for v0 in self.intensities.iter() {
            v0.encode_one(buf)?;
        }
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let ranges_length = buf.read_i32::<BigEndian>()? as usize;
        let intensities_length = buf.read_i32::<BigEndian>()? as usize;
        let header = crate::std_msgs::Header::decode_one(buf)?;
        let angle_min = buf.read_f32::<BigEndian>()?;
        let angle_max = buf.read_f32::<BigEndian>()?;
        let angle_increment = buf.read_f32::<BigEndian>()?;
        let time_increment = buf.read_f32::<BigEndian>()?;
        let scan_time = buf.read_f32::<BigEndian>()?;
        let range_min = buf.read_f32::<BigEndian>()?;
        let range_max = buf.read_f32::<BigEndian>()?;
        let ranges = {
            let mut v = Vec::with_capacity(ranges_length);
            for _ in 0..ranges_length {
                let _elem_0 = crate::sensor_msgs::LaserEcho::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        let intensities = {
            let mut v = Vec::with_capacity(intensities_length);
            for _ in 0..intensities_length {
                let _elem_0 = crate::sensor_msgs::LaserEcho::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        Ok(Self {
            header,
            angle_min,
            angle_max,
            angle_increment,
            time_increment,
            scan_time,
            range_min,
            range_max,
            ranges,
            intensities,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4;
        size += 4;
        size += self.header.encoded_size();
        size += 4;
        size += 4;
        size += 4;
        size += 4;
        size += 4;
        size += 4;
        size += 4;
        for v0 in self.ranges.iter() {
            size += v0.encoded_size();
        }
        for v0 in self.intensities.iter() {
            size += v0.encoded_size();
        }
        size
    }
}
