// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Cursor, Read, Write};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct PointCloud {
    pub timestamp: crate::builtin_interfaces::Time,
    pub frame_id: std::string::String,
    pub pose: crate::geometry_msgs::Pose,
    pub point_stride: i32,
    pub fields: Vec<crate::foxglove_msgs::PackedElementField>,
    pub data: Vec<u8>,
}

impl PointCloud {
    pub const HASH: i64 = 0x4F3713AA9B8DC878u64 as i64;
    pub const NAME: &str = "foxglove_msgs.PointCloud";

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let mut tmphash = self_hash as u64;
        tmphash = tmphash.wrapping_add(crate::builtin_interfaces::Time::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::geometry_msgs::Pose::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::foxglove_msgs::PackedElementField::hash_recursive(
            parents,
        ));
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint())
            .unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(
                io::ErrorKind::InvalidData,
                format!(
                    "Hash mismatch: expected {:016x}, got {:016x}",
                    expected, hash
                ),
            ));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        buf.write_i32::<BigEndian>(self.fields.len() as i32)?;
        buf.write_i32::<BigEndian>(self.data.len() as i32)?;
        self.timestamp.encode_one(buf)?;
        {
            let bytes = self.frame_id.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        self.pose.encode_one(buf)?;
        buf.write_i32::<BigEndian>(self.point_stride)?;
        for v0 in self.fields.iter() {
            v0.encode_one(buf)?;
        }
        buf.write_all(&self.data)?;
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let fields_length = buf.read_i32::<BigEndian>()? as usize;
        let data_length = buf.read_i32::<BigEndian>()? as usize;
        let timestamp = crate::builtin_interfaces::Time::decode_one(buf)?;
        let frame_id = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        let pose = crate::geometry_msgs::Pose::decode_one(buf)?;
        let point_stride = buf.read_i32::<BigEndian>()?;
        let fields = {
            let mut v = Vec::with_capacity(fields_length);
            for _ in 0..fields_length {
                let _elem_0 = crate::foxglove_msgs::PackedElementField::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        let data = {
            let mut v = vec![0u8; data_length];
            buf.read_exact(&mut v)?;
            v
        };
        Ok(Self {
            timestamp,
            frame_id,
            pose,
            point_stride,
            fields,
            data,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4;
        size += 4;
        size += self.timestamp.encoded_size();
        size += 4 + self.frame_id.len() + 1;
        size += self.pose.encoded_size();
        size += 4;
        for v0 in self.fields.iter() {
            size += v0.encoded_size();
        }
        size += self.data.len();
        size
    }
}
