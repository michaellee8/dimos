// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Cursor, Read, Write};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct JointTrajectoryPoint {
    pub positions: Vec<f64>,
    pub velocities: Vec<f64>,
    pub accelerations: Vec<f64>,
    pub effort: Vec<f64>,
    pub time_from_start: crate::std_msgs::Duration,
}

impl JointTrajectoryPoint {
    pub const HASH: i64 = 0x5F17DCAE9DA98292u64 as i64;
    pub const NAME: &str = "trajectory_msgs.JointTrajectoryPoint";

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let mut tmphash = self_hash as u64;
        tmphash = tmphash.wrapping_add(crate::std_msgs::Duration::hash_recursive(parents));
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint())
            .unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(
                io::ErrorKind::InvalidData,
                format!(
                    "Hash mismatch: expected {:016x}, got {:016x}",
                    expected, hash
                ),
            ));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        buf.write_i32::<BigEndian>(self.positions.len() as i32)?;
        buf.write_i32::<BigEndian>(self.velocities.len() as i32)?;
        buf.write_i32::<BigEndian>(self.accelerations.len() as i32)?;
        buf.write_i32::<BigEndian>(self.effort.len() as i32)?;
        for v0 in self.positions.iter() {
            buf.write_f64::<BigEndian>(*v0)?;
        }
        for v0 in self.velocities.iter() {
            buf.write_f64::<BigEndian>(*v0)?;
        }
        for v0 in self.accelerations.iter() {
            buf.write_f64::<BigEndian>(*v0)?;
        }
        for v0 in self.effort.iter() {
            buf.write_f64::<BigEndian>(*v0)?;
        }
        self.time_from_start.encode_one(buf)?;
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let positions_length = buf.read_i32::<BigEndian>()? as usize;
        let velocities_length = buf.read_i32::<BigEndian>()? as usize;
        let accelerations_length = buf.read_i32::<BigEndian>()? as usize;
        let effort_length = buf.read_i32::<BigEndian>()? as usize;
        let positions = {
            let mut v = Vec::with_capacity(positions_length);
            for _ in 0..positions_length {
                let _elem_0 = buf.read_f64::<BigEndian>()?;
                v.push(_elem_0);
            }
            v
        };
        let velocities = {
            let mut v = Vec::with_capacity(velocities_length);
            for _ in 0..velocities_length {
                let _elem_0 = buf.read_f64::<BigEndian>()?;
                v.push(_elem_0);
            }
            v
        };
        let accelerations = {
            let mut v = Vec::with_capacity(accelerations_length);
            for _ in 0..accelerations_length {
                let _elem_0 = buf.read_f64::<BigEndian>()?;
                v.push(_elem_0);
            }
            v
        };
        let effort = {
            let mut v = Vec::with_capacity(effort_length);
            for _ in 0..effort_length {
                let _elem_0 = buf.read_f64::<BigEndian>()?;
                v.push(_elem_0);
            }
            v
        };
        let time_from_start = crate::std_msgs::Duration::decode_one(buf)?;
        Ok(Self {
            positions,
            velocities,
            accelerations,
            effort,
            time_from_start,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4;
        size += 4;
        size += 4;
        size += 4;
        size += self.positions.len() * 8;
        size += self.velocities.len() * 8;
        size += self.accelerations.len() * 8;
        size += self.effort.len() * 8;
        size += self.time_from_start.encoded_size();
        size
    }
}
