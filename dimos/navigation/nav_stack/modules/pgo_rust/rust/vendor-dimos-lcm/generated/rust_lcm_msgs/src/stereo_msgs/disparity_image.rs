// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Cursor, Read, Write};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct DisparityImage {
    pub header: crate::std_msgs::Header,
    pub image: crate::sensor_msgs::Image,
    pub f: f32,
    pub T: f32,
    pub valid_window: crate::sensor_msgs::RegionOfInterest,
    pub min_disparity: f32,
    pub max_disparity: f32,
    pub delta_d: f32,
}

impl DisparityImage {
    pub const HASH: i64 = 0xDEB7A557A2B9258Au64 as i64;
    pub const NAME: &str = "stereo_msgs.DisparityImage";

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let mut tmphash = self_hash as u64;
        tmphash = tmphash.wrapping_add(crate::std_msgs::Header::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::sensor_msgs::Image::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::sensor_msgs::RegionOfInterest::hash_recursive(
            parents,
        ));
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint())
            .unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(
                io::ErrorKind::InvalidData,
                format!(
                    "Hash mismatch: expected {:016x}, got {:016x}",
                    expected, hash
                ),
            ));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        self.header.encode_one(buf)?;
        self.image.encode_one(buf)?;
        buf.write_f32::<BigEndian>(self.f)?;
        buf.write_f32::<BigEndian>(self.T)?;
        self.valid_window.encode_one(buf)?;
        buf.write_f32::<BigEndian>(self.min_disparity)?;
        buf.write_f32::<BigEndian>(self.max_disparity)?;
        buf.write_f32::<BigEndian>(self.delta_d)?;
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let header = crate::std_msgs::Header::decode_one(buf)?;
        let image = crate::sensor_msgs::Image::decode_one(buf)?;
        let f = buf.read_f32::<BigEndian>()?;
        let T = buf.read_f32::<BigEndian>()?;
        let valid_window = crate::sensor_msgs::RegionOfInterest::decode_one(buf)?;
        let min_disparity = buf.read_f32::<BigEndian>()?;
        let max_disparity = buf.read_f32::<BigEndian>()?;
        let delta_d = buf.read_f32::<BigEndian>()?;
        Ok(Self {
            header,
            image,
            f,
            T,
            valid_window,
            min_disparity,
            max_disparity,
            delta_d,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += self.header.encoded_size();
        size += self.image.encoded_size();
        size += 4;
        size += 4;
        size += self.valid_window.encoded_size();
        size += 4;
        size += 4;
        size += 4;
        size
    }
}
