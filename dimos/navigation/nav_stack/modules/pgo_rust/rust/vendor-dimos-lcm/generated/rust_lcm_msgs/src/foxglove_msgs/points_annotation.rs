// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Cursor, Read, Write};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct PointsAnnotation {
    pub timestamp: crate::builtin_interfaces::Time,
    pub r#type: u8,
    pub points: Vec<crate::foxglove_msgs::Point2>,
    pub outline_color: crate::foxglove_msgs::Color,
    pub outline_colors: Vec<crate::foxglove_msgs::Color>,
    pub fill_color: crate::foxglove_msgs::Color,
    pub thickness: f64,
}

impl PointsAnnotation {
    pub const HASH: i64 = 0x97465363CC7C2A18u64 as i64;
    pub const NAME: &str = "foxglove_msgs.PointsAnnotation";

    pub const UNKNOWN: i8 = 0;
    pub const POINTS: i8 = 1;
    pub const LINE_LOOP: i8 = 2;
    pub const LINE_STRIP: i8 = 3;
    pub const LINE_LIST: i8 = 4;

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let mut tmphash = self_hash as u64;
        tmphash = tmphash.wrapping_add(crate::builtin_interfaces::Time::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::foxglove_msgs::Point2::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::foxglove_msgs::Color::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::foxglove_msgs::Color::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::foxglove_msgs::Color::hash_recursive(parents));
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint())
            .unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(
                io::ErrorKind::InvalidData,
                format!(
                    "Hash mismatch: expected {:016x}, got {:016x}",
                    expected, hash
                ),
            ));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        buf.write_i32::<BigEndian>(self.points.len() as i32)?;
        buf.write_i32::<BigEndian>(self.outline_colors.len() as i32)?;
        self.timestamp.encode_one(buf)?;
        buf.write_u8(self.r#type)?;
        for v0 in self.points.iter() {
            v0.encode_one(buf)?;
        }
        self.outline_color.encode_one(buf)?;
        for v0 in self.outline_colors.iter() {
            v0.encode_one(buf)?;
        }
        self.fill_color.encode_one(buf)?;
        buf.write_f64::<BigEndian>(self.thickness)?;
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let points_length = buf.read_i32::<BigEndian>()? as usize;
        let outline_colors_length = buf.read_i32::<BigEndian>()? as usize;
        let timestamp = crate::builtin_interfaces::Time::decode_one(buf)?;
        let r#type = buf.read_u8()?;
        let points = {
            let mut v = Vec::with_capacity(points_length);
            for _ in 0..points_length {
                let _elem_0 = crate::foxglove_msgs::Point2::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        let outline_color = crate::foxglove_msgs::Color::decode_one(buf)?;
        let outline_colors = {
            let mut v = Vec::with_capacity(outline_colors_length);
            for _ in 0..outline_colors_length {
                let _elem_0 = crate::foxglove_msgs::Color::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        let fill_color = crate::foxglove_msgs::Color::decode_one(buf)?;
        let thickness = buf.read_f64::<BigEndian>()?;
        Ok(Self {
            timestamp,
            r#type,
            points,
            outline_color,
            outline_colors,
            fill_color,
            thickness,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4;
        size += 4;
        size += self.timestamp.encoded_size();
        size += 1;
        for v0 in self.points.iter() {
            size += v0.encoded_size();
        }
        size += self.outline_color.encoded_size();
        for v0 in self.outline_colors.iter() {
            size += v0.encoded_size();
        }
        size += self.fill_color.encoded_size();
        size += 8;
        size
    }
}
