// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Cursor, Read, Write};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Imu {
    pub header: crate::std_msgs::Header,
    pub orientation: crate::geometry_msgs::Quaternion,
    pub orientation_covariance: [f64; 9],
    pub angular_velocity: crate::geometry_msgs::Vector3,
    pub angular_velocity_covariance: [f64; 9],
    pub linear_acceleration: crate::geometry_msgs::Vector3,
    pub linear_acceleration_covariance: [f64; 9],
}

impl Imu {
    pub const HASH: i64 = 0x55C1E238541325F6u64 as i64;
    pub const NAME: &str = "sensor_msgs.Imu";

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let mut tmphash = self_hash as u64;
        tmphash = tmphash.wrapping_add(crate::std_msgs::Header::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::geometry_msgs::Quaternion::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::geometry_msgs::Vector3::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::geometry_msgs::Vector3::hash_recursive(parents));
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint())
            .unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(
                io::ErrorKind::InvalidData,
                format!(
                    "Hash mismatch: expected {:016x}, got {:016x}",
                    expected, hash
                ),
            ));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        self.header.encode_one(buf)?;
        self.orientation.encode_one(buf)?;
        for v0 in self.orientation_covariance.iter() {
            buf.write_f64::<BigEndian>(*v0)?;
        }
        self.angular_velocity.encode_one(buf)?;
        for v0 in self.angular_velocity_covariance.iter() {
            buf.write_f64::<BigEndian>(*v0)?;
        }
        self.linear_acceleration.encode_one(buf)?;
        for v0 in self.linear_acceleration_covariance.iter() {
            buf.write_f64::<BigEndian>(*v0)?;
        }
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let header = crate::std_msgs::Header::decode_one(buf)?;
        let orientation = crate::geometry_msgs::Quaternion::decode_one(buf)?;
        let orientation_covariance = {
            let mut _arr_0 = [0.0f64; 9];
            for elem in _arr_0.iter_mut() {
                *elem = buf.read_f64::<BigEndian>()?;
            }
            _arr_0
        };
        let angular_velocity = crate::geometry_msgs::Vector3::decode_one(buf)?;
        let angular_velocity_covariance = {
            let mut _arr_0 = [0.0f64; 9];
            for elem in _arr_0.iter_mut() {
                *elem = buf.read_f64::<BigEndian>()?;
            }
            _arr_0
        };
        let linear_acceleration = crate::geometry_msgs::Vector3::decode_one(buf)?;
        let linear_acceleration_covariance = {
            let mut _arr_0 = [0.0f64; 9];
            for elem in _arr_0.iter_mut() {
                *elem = buf.read_f64::<BigEndian>()?;
            }
            _arr_0
        };
        Ok(Self {
            header,
            orientation,
            orientation_covariance,
            angular_velocity,
            angular_velocity_covariance,
            linear_acceleration,
            linear_acceleration_covariance,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += self.header.encoded_size();
        size += self.orientation.encoded_size();
        size += self.orientation_covariance.len() * 8;
        size += self.angular_velocity.encoded_size();
        size += self.angular_velocity_covariance.len() * 8;
        size += self.linear_acceleration.encoded_size();
        size += self.linear_acceleration_covariance.len() * 8;
        size
    }
}
