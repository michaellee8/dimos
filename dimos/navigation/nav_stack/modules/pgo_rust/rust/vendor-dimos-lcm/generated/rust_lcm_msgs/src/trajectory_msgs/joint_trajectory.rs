// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Cursor, Read, Write};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct JointTrajectory {
    pub header: crate::std_msgs::Header,
    pub joint_names: Vec<std::string::String>,
    pub points: Vec<crate::trajectory_msgs::JointTrajectoryPoint>,
}

impl JointTrajectory {
    pub const HASH: i64 = 0xF09DB31AFE774086u64 as i64;
    pub const NAME: &str = "trajectory_msgs.JointTrajectory";

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let mut tmphash = self_hash as u64;
        tmphash = tmphash.wrapping_add(crate::std_msgs::Header::hash_recursive(parents));
        tmphash = tmphash
            .wrapping_add(crate::trajectory_msgs::JointTrajectoryPoint::hash_recursive(parents));
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint())
            .unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(
                io::ErrorKind::InvalidData,
                format!(
                    "Hash mismatch: expected {:016x}, got {:016x}",
                    expected, hash
                ),
            ));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        buf.write_i32::<BigEndian>(self.joint_names.len() as i32)?;
        buf.write_i32::<BigEndian>(self.points.len() as i32)?;
        self.header.encode_one(buf)?;
        for v0 in self.joint_names.iter() {
            {
                let bytes = v0.as_bytes();
                buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
                buf.write_all(bytes)?;
                buf.write_u8(0)?;
            }
        }
        for v0 in self.points.iter() {
            v0.encode_one(buf)?;
        }
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let joint_names_length = buf.read_i32::<BigEndian>()? as usize;
        let points_length = buf.read_i32::<BigEndian>()? as usize;
        let header = crate::std_msgs::Header::decode_one(buf)?;
        let joint_names = {
            let mut v = Vec::with_capacity(joint_names_length);
            for _ in 0..joint_names_length {
                let _elem_0 = {
                    let len = buf.read_u32::<BigEndian>()? as usize;
                    let mut bytes = vec![0u8; len];
                    buf.read_exact(&mut bytes)?;
                    std::string::String::from_utf8(bytes[..len - 1].to_vec())
                        .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
                };
                v.push(_elem_0);
            }
            v
        };
        let points = {
            let mut v = Vec::with_capacity(points_length);
            for _ in 0..points_length {
                let _elem_0 = crate::trajectory_msgs::JointTrajectoryPoint::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        Ok(Self {
            header,
            joint_names,
            points,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4;
        size += 4;
        size += self.header.encoded_size();
        for s0 in self.joint_names.iter() {
            size += 4 + s0.len() + 1;
        }
        for v0 in self.points.iter() {
            size += v0.encoded_size();
        }
        size
    }
}
