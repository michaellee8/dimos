// Auto-generated by lcm-rust-gen. DO NOT EDIT.

use byteorder::{BigEndian, ReadBytesExt, WriteBytesExt};
use std::io::{self, Cursor, Read, Write};
use std::sync::OnceLock;

#[derive(Debug, Clone, Default, PartialEq)]
pub struct InteractiveMarkerControl {
    pub name: std::string::String,
    pub orientation: crate::geometry_msgs::Quaternion,
    pub orientation_mode: u8,
    pub interaction_mode: u8,
    pub always_visible: bool,
    pub markers: Vec<crate::visualization_msgs::Marker>,
    pub independent_marker_orientation: bool,
    pub description: std::string::String,
}

impl InteractiveMarkerControl {
    pub const HASH: i64 = 0x9E1DB7D7740DA609u64 as i64;
    pub const NAME: &str = "visualization_msgs.InteractiveMarkerControl";

    pub const INHERIT: i8 = 0;
    pub const FIXED: i8 = 1;
    pub const VIEW_FACING: i8 = 2;
    pub const NONE: i8 = 0;
    pub const MENU: i8 = 1;
    pub const BUTTON: i8 = 2;
    pub const MOVE_AXIS: i8 = 3;
    pub const MOVE_PLANE: i8 = 4;
    pub const ROTATE_AXIS: i8 = 5;
    pub const MOVE_ROTATE: i8 = 6;
    pub const MOVE_3D: i8 = 7;
    pub const ROTATE_3D: i8 = 8;
    pub const MOVE_ROTATE_3D: i8 = 9;

    fn packed_fingerprint() -> u64 {
        static CACHE: OnceLock<u64> = OnceLock::new();
        *CACHE.get_or_init(|| Self::hash_recursive(&mut Vec::new()))
    }

    pub(crate) fn hash_recursive(parents: &mut Vec<u64>) -> u64 {
        let self_hash = Self::HASH as u64;
        if parents.contains(&self_hash) {
            return 0;
        }
        parents.push(self_hash);
        let mut tmphash = self_hash as u64;
        tmphash = tmphash.wrapping_add(crate::geometry_msgs::Quaternion::hash_recursive(parents));
        tmphash = tmphash.wrapping_add(crate::visualization_msgs::Marker::hash_recursive(parents));
        parents.pop();
        // rotate left by 1
        tmphash << 1 | tmphash >> 63
    }

    pub fn encode(&self) -> Vec<u8> {
        let mut buf = Vec::with_capacity(8 + self.encoded_size());
        buf.write_u64::<BigEndian>(Self::packed_fingerprint())
            .unwrap();
        self.encode_one(&mut buf).unwrap();
        buf
    }

    pub fn decode(data: &[u8]) -> io::Result<Self> {
        let mut cursor = Cursor::new(data);
        let hash = cursor.read_u64::<BigEndian>()?;
        let expected = Self::packed_fingerprint();
        if hash != expected {
            return Err(io::Error::new(
                io::ErrorKind::InvalidData,
                format!(
                    "Hash mismatch: expected {:016x}, got {:016x}",
                    expected, hash
                ),
            ));
        }
        Self::decode_one(&mut cursor)
    }

    pub fn encode_one<W: Write>(&self, buf: &mut W) -> io::Result<()> {
        buf.write_i32::<BigEndian>(self.markers.len() as i32)?;
        {
            let bytes = self.name.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        self.orientation.encode_one(buf)?;
        buf.write_u8(self.orientation_mode)?;
        buf.write_u8(self.interaction_mode)?;
        buf.write_i8(if self.always_visible { 1 } else { 0 })?;
        for v0 in self.markers.iter() {
            v0.encode_one(buf)?;
        }
        buf.write_i8(if self.independent_marker_orientation {
            1
        } else {
            0
        })?;
        {
            let bytes = self.description.as_bytes();
            buf.write_u32::<BigEndian>((bytes.len() + 1) as u32)?;
            buf.write_all(bytes)?;
            buf.write_u8(0)?;
        }
        Ok(())
    }

    pub fn decode_one<R: Read>(buf: &mut R) -> io::Result<Self> {
        let markers_length = buf.read_i32::<BigEndian>()? as usize;
        let name = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        let orientation = crate::geometry_msgs::Quaternion::decode_one(buf)?;
        let orientation_mode = buf.read_u8()?;
        let interaction_mode = buf.read_u8()?;
        let always_visible = buf.read_i8()? != 0;
        let markers = {
            let mut v = Vec::with_capacity(markers_length);
            for _ in 0..markers_length {
                let _elem_0 = crate::visualization_msgs::Marker::decode_one(buf)?;
                v.push(_elem_0);
            }
            v
        };
        let independent_marker_orientation = buf.read_i8()? != 0;
        let description = {
            let len = buf.read_u32::<BigEndian>()? as usize;
            let mut bytes = vec![0u8; len];
            buf.read_exact(&mut bytes)?;
            std::string::String::from_utf8(bytes[..len - 1].to_vec())
                .map_err(|e| io::Error::new(io::ErrorKind::InvalidData, e))?
        };
        Ok(Self {
            name,
            orientation,
            orientation_mode,
            interaction_mode,
            always_visible,
            markers,
            independent_marker_orientation,
            description,
        })
    }

    pub fn encoded_size(&self) -> usize {
        let mut size = 0usize;
        size += 4;
        size += 4 + self.name.len() + 1;
        size += self.orientation.encoded_size();
        size += 1;
        size += 1;
        size += 1;
        for v0 in self.markers.iter() {
            size += v0.encoded_size();
        }
        size += 1;
        size += 4 + self.description.len() + 1;
        size
    }
}
